# 行尾策略（与 .editorconfig 配套）：
#   - 仓库内（index）一律 LF —— `* text=auto eol=lf` 双重保证；
#   - 即使某台机器 core.autocrlf 关闭/异常，提交进仓库的内容也不会出现 CRLF/混合行尾；
#   - 二进制文件列在下方显式豁免（后声明覆盖先声明）。
* text=auto eol=lf

# —— 二进制资产：永不参与行尾转换 ——
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.webp binary
*.ico binary
*.jar binary
*.apk binary
*.jks binary
*.keystore binary

# —— Windows cmd 入口：必须保持 CRLF（bat 对纯 LF 行尾存在兼容问题）——
*.bat text eol=crlf
*.cmd text eol=crlf
