# 2026-08-20

## 净启动 AdSkip — 开屏广告自动跳过 App（Android）
- 在 Ui-APP 仓库从零搭建完整 Android 项目并成功编译出 APK（836KB，零第三方依赖，Kotlin + 纯框架 API）。
- 方案：AccessibilityService 监听窗口事件 → 遍历节点树按关键词（跳过/skip 等，≤12字、非输入框）匹配 → 父链找 clickable 节点点击，兜底坐标手势。防误触：同应用 1.2s 去抖、150ms 全局节流、500 节点上限、忽略 systemui。
- 文件：settings.gradle.kts（阿里云镜像）、app/build.gradle.kts（AGP 8.7.3 / Kotlin 2.0.21 / compileSdk 35 / minSdk 26）、SkipAdService.kt、MainActivity.kt（含模拟开屏广告自测）、Prefs.kt。
- 产物：AdSkip-v1.0.apk（项目根目录）。
- 构建环境备忘：JDK 在 `C:\Users\LDP\.jdks\corretto-22.0.2`（Program Files 下没有 JDK）；Gradle 8.11.1 已解压在 `C:\Users\LDP\.workbuddy\tools\gradle-8.11.1`；Android SDK 在 `C:\Users\LDP\AppData\Local\Android\Sdk`（API 34/35/36.1）；Gradle 发行版可从腾讯镜像 mirrors.cloud.tencent.com/gradle 下载。
- 命令：`JAVA_HOME='C:\Users\LDP\.jdks\corretto-22.0.2' "C:/Users/LDP/.workbuddy/tools/gradle-8.11.1/bin/gradle.bat" assembleDebug --no-daemon`

## v2.0 全栈升级（同日完成）
- 客户端：规则引擎升级（文本关键词 + ViewID 双通道，支持纯图片跳过按钮）、应用管理页（逐项开关+计数）、跳过日志页（200 条上限）、云端规则同步页（SyncUtil，HttpURLConnection 零依赖）；新增 INTERNET 权限与 queries(LAUNCHER) 声明。
- 后端：server/ 目录，Node.js 原生 http 零依赖（端口 3210）：GET /api/rules/latest、PUT /api/rules、POST /api/skip、GET /api/stats/summary + 管理后台 public/admin.html（规则编辑发布 + 统计看板）。数据在 server/data/*.json。
- 后端启动命令：`cd server && node server.js`；本机局域网 IP：192.168.31.132（手机端填 http://192.168.31.132:3210）。
- 产物：AdSkip-v2.0.apk（项目根目录）；文档：ROADMAP.md、README.md 已更新。
- 坑：改 strings.xml 时删掉了 app_slogan/btn_add/how_it_works 导致 AAPT 链接失败——重写资源文件前要先 grep 全部 @string/ 引用。

## 产品前端落地页（同日完成）
- server/public/index.html：单文件干净简洁风落地页（品牌蓝 #1565C0），含 Hero、CSS 手机模型动态演示（广告出现→光标点击跳过→Toast，5.2s 循环）、功能 6 卡、工作原理 4 步、路线图时间线、CTA、合规页脚；IntersectionObserver 滚动渐显，响应式（860/560px 断点）。
- server.js 新路由：GET / → 落地页，/admin → 管理后台，/download → 流式下载 AdSkip-v2.0.apk（content-disposition attachment）。
- 重启服务方式：PowerShell `Get-NetTCPConnection -LocalPort 3210 -State Listen` 拿 PID 后 Stop-Process，再后台重启 node server.js。
- 注意：Git Bash 下 curl -o /dev/null 会 exit 23（写错误），改用临时文件验证。

## 架构升级（同日完成，行为不变）
- 客户端分层：ui/（4 Activity）、service/SkipAdService（薄编排）、engine/（SkipRuleEngine 纯匹配 + RuleSet 模型）、data/（Prefs 原语 + RulesRepository 规则合并/云端落地 + StatsRepository 计数/日志）、net/SyncClient（传输+解析，落地委托仓库）。Manifest 组件路径改 .ui.*/.service.*。
- 服务端模块化：server.js 仅引导，src/{config,httpUtil,store,api}.js；store 用 tmp+rename 原子写；新增 test/smoke.js 冒烟测试（npm test，7 项全过）。
- 新增 ARCHITECTURE.md（全景图/分层职责/数据流/设计决策/扩展点）。
- 重要环境坑：用户在 Android Studio 打开了本项目（.idea、.gradle/9.7.0），AS 的 Gradle 守护进程长期持有 .gradle/buildOutputCleanup 锁 → 命令行构建报"拒绝访问"。解法：`gradle assembleDebug --project-cache-dir .gradle-cli`（已加入 .gitignore）。沙箱会拦截 rm/mv 这些目录（fail-closed），不要硬删 .gradle。

## 提交推送至 GitHub（同日完成）
- 远程：git@github.com:LiYihai1121/ui-app.git（原 HTTPS 在本机被墙连不上，已改用 SSH，密钥已配好可用）。
- 本地历史与远程原本是两条独立历史（无共同祖先）→ 按用户选择用 `git merge -s ours origin/main --allow-unrelated-histories` 合并，保留双方历史且以本地完整版为准。
- 推送后本地 refs/remotes/origin/main 一度丢失（fetch 报 [new branch] 但未落盘），需 `git fetch origin "+refs/heads/*:refs/remotes/origin/*"` 显式 refspec 才写盘。最终 origin/main == HEAD == 6cf95d8，完全同步。
- 遗留：本地 tag AdSkip-v2 仍指向旧提交 0213d5f，如需指向最终版可 `git tag -f AdSkip-v2 6cf95d8 && git push -f origin AdSkip-v2`。
